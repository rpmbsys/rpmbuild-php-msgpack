version: "3.5"
services:
  centos6msgpackphp5build:
    networks:
      - buildnet
    build:
      context: .
      args:
        centos: 6
        image: php-igbinary-5.6
      network: buildnet
    image: aursu/peclbuild:6-php-msgpack-5.6
  centos7msgpackphp5build:
    networks:
      - buildnet
    build:
      context: .
      args:
        image: php-igbinary-5.6
      network: buildnet
    image: aursu/peclbuild:7-php-msgpack-5.6

networks:
  buildnet:
    external: true
    name: buildnet
